# Simple Chaster control script example

import requests

# Replace with your token
def unlock_lock(lock_id, code):
    # Placeholder for API call
    print(f'Unlocking {lock_id} with code: {code}')

# Usage
# unlock_lock('your_lock_id', 'ENDLOCK_MAAM_APPROVED')